{
  "product_id": "hestia",
  "capability_id": "hestia.host.deploy@v1",
  "name": "HESTIA deploy",
  "description": "Start an isolated AIMarket capability provider on this hearth. Not a Hub listing. Not a job board.",
  "invoke_url": "https://hestia.modelmarket.dev/ai-market/v2/invoke",
  "publisher_id": "hestia",
  "provider_pubkey": "",
  "price_per_call_usd": 0.05,
  "input_schema": {
    "type": "object",
    "required": ["slug", "capability", "source", "owner_pubkey"],
    "properties": {
      "slug": { "type": "string", "minLength": 3, "maxLength": 48 },
      "capability": { "type": "object" },
      "source": { "type": "object" },
      "owner_pubkey": { "type": "string" },
      "announce": { "type": "boolean" },
      "note": { "type": "string", "maxLength": 280 }
    },
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "required": ["ok", "slug", "public_url", "announced"],
    "properties": {
      "ok": { "type": "boolean" },
      "slug": { "type": "string" },
      "public_url": { "type": "string" },
      "announced": { "type": "boolean" }
    },
    "additionalProperties": true
  }
}
